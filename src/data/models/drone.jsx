import { droneModels, droneStates } from "../../constants/constants";

export const droneModel = {
    id: null,
    serial_number: 0,
    model: droneModels[0],
    max_weight: 0,
    battery_capacity: 0,
    state: droneStates[0]
}